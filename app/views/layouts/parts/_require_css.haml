-content_for :head do
  = stylesheet_link_tag "views/#{cssname}",:media=>'screen, projection'
  - if iefix
    /[if IE]
      = stylesheet_link_tag "views/#{cssname}.ie",:media=>'screen, projection'